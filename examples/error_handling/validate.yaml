target:
  program: "examples/error_handling/validate.exe"
  timeout: 5

wrapper:
  enabled: true
  mode: "args"
  schema:
    - name: "number"
      type: "int"
      range: [-10, 150]

assertions:
  - type: "exit_code"
    expected: [0, 1, 2, 3]
